<UserControl
    x:Class="PSK.PullResfreshListview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PSK"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Border x:Name="_root">
        <Grid>
            <Border x:Name="_head_border"
                    VerticalAlignment="Top"
                    SizeChanged="_head_border_SizeChanged">
                <Grid>
                    <ContentPresenter x:Name="_head_content"
                                      HorizontalAlignment="Center"/>
                </Grid>
                <Border.RenderTransform>
                    <CompositeTransform x:Name="_head_renderT" />
                </Border.RenderTransform>
            </Border>
            <ScrollViewer x:Name="_ScrollViewer"
                          AutomationProperties.AccessibilityView="Raw"
                          >
                <ItemsPresenter x:Name="_ItemsPresenter"/>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
