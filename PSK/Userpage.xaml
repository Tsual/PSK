<Page
    x:Class="PSK.Userpage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PSK"
    xmlns:model="using:PSK.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.Resources>
            <DataTemplate x:Key="ListItem_Templete_Modify" x:DataType="model:Info">
                <Grid Margin="0,0,0,10"
                      HorizontalAlignment="Stretch">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <TextBox Text="{x:Bind DetailName,Mode=TwoWay }"
                               Grid.Column="0"
                               FontSize="18"
                             IsEnabled="{x:Bind Switchbool}"
                             BorderThickness="0,0,0,1"></TextBox>
                    <ToggleSwitch Grid.Column="1"
                                  Margin="10,0,0,0"
                                  OnContent="修改"
                                  OffContent="修改"
                                  IsOn="{x:Bind Switchbool,Mode=TwoWay}"
                                  Toggled="ToggleSwitch_Toggled"></ToggleSwitch>
                    <TextBox Text="{x:Bind Detail , Mode=TwoWay}"
                               Grid.ColumnSpan="2"
                               Grid.Row="2"
                               FontSize="14"
                               Margin="5"
                             IsEnabled="{x:Bind Switchbool}"
                             BorderThickness="0,0,0,1"></TextBox>

                </Grid>
            </DataTemplate>
        </Grid.Resources>
        <ListView x:Name="_ListView" 
                  ItemsSource="{x:Bind ItemCollection }"
                  ItemTemplate="{StaticResource ListItem_Templete_Modify}"></ListView>
        <Button Click="Button_Click" Content="back"/>
    </Grid>
</Page>
