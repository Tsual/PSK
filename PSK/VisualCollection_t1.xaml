<UserControl
    x:Class="PSK.VisualCollection_t1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PSK"

    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="9*"/>
        </Grid.ColumnDefinitions>
        <ScrollBar Grid.ColumnSpan="2" x:Name="c_ScrollBar" HorizontalAlignment="Right" VerticalAlignment="Stretch" Holding="c_ScrollBar_Holding"/>
        <Grid x:Name="MainPanel" Grid.Column="1">
            
        </Grid>
    </Grid>
</UserControl>
